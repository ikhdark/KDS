# Implementation Plan

KDS V1 is a public automatic-hook-first command evidence runner. Implement the
CLI, storage, sidecars, run index, summaries, digest cache, gain metrics,
doctor checks, Codex init, PowerShell hooks, installers, docs, and CI described
in `README.md`, `docs/DECISIONS.md`, and `docs/VALIDATION.md`.
